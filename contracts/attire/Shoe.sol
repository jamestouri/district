pragma solidity >=0.4.21 <0.6.0;

contract Shoe {
  uint[3] = _volume;
  constructor(uint256 length, uint256 width, uint256 height) public {
    _volume[0] = length;
    _volume[1] = width;
    _volume[2] = height;
  }
}
